# Nofar Software — מערכת ניהול מטפל–מטופל

מערכת Web לניהול הקשר הטיפולי בין מטפל יחיד למטופליו בתחומי נטורופתיה / רפלקסולוגיה / תזונה.
עברית RTL, responsive, ללא אפליקציה native.

## כלל הזהב

> **כל מטופל רואה אך ורק את המידע שלו.** אף מטופל לא יכול להגיע למידע של מטופל אחר —
> לא ב-UI, לא בניחוש URL, לא ב-API, ולא בזיוף מזהה/בקשה. זו הדרישה הקריטית מספר 1.
> כל שינוי בקוד נבחן קודם מול הכלל הזה.

## מסמכי הקונטקסט — קרא לפי הצורך

| קובץ | מתי לקרוא |
|------|-----------|
| [`docs/STATUS.md`](docs/STATUS.md) | **בתחילת כל סשן.** מצב נוכחי, מה הבא, בעיות פתוחות, יומן סשנים |
| [`docs/PROJECT_BRIEF.md`](docs/PROJECT_BRIEF.md) | אפיון מלא, מילון מונחים, סקירת שוק והצדקת הבנייה |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | סטאק, חלוקה למודולים, אסטרטגיית בידוד מידע, מבנה תיקיות, קונבנציות |
| [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md) | ישויות וקשרים |
| [`docs/DECISIONS.md`](docs/DECISIONS.md) | לוג החלטות (ADR) — append-only. מה, מתי, למה, אילו חלופות נדחו |
| [`docs/WORK_PACKAGES.md`](docs/WORK_PACKAGES.md) | חבילות עבודה: סטטוס, תלויות, הגדרת "גמור" |

## נוהל עבודה בכל סשן

1. **התחלה:** קרא `docs/STATUS.md`. אם נוגעים בארכיטקטורה/נתונים — קרא גם את המסמך הרלוונטי.
2. **במהלך:** כל החלטה משמעותית → רשומת ADR חדשה ב-`DECISIONS.md` (לא לערוך ישנות).
3. **סיום:** עדכן את `docs/STATUS.md` — "מה נעשה / מה בעבודה / מה הבא / בעיות פתוחות" + שורת יומן סשן בתחתית.

## עקרונות פיתוח

- **מודולריות:** core משותף + מודולי דומיין. מודול לא מייבא internals של מודול אחר — רק דרך ה-service function הציבורי שלו.
- **שינוי אחד לא שובר הכל:** גבולות קשיחים סביב שני דברים בלבד — ה-scoping guard וה-validation layer של השדות הגמישים. שאר הגבולות רכים ופרקטיים.
- **בידוד = executable:** לכל מודול יש בדיקת cross-tenant ב-CI. הבדיקה היא ההגדרה של "גמור" לדרישה הקריטית.
- **סקופ:** MVP מצומצם (ראה `WORK_PACKAGES.md`). לא מקבעים מבנים שלא סופקו סופית — שדות דינמיים דרך Field Registry.
- **עיצוב לפני קוד:** מסך חדש = קודם מוקאפ מאושר, אז מימוש.

## סטאק (מקובע — ראה ADR)

Next.js App Router · Vercel · Postgres (Vercel Marketplace / Neon, אזור פרנקפורט) · Auth.js self-hosted · Tailwind + shadcn/ui (RTL) · שפה ויזואלית "Calm Wellness" · Desktop-first.
